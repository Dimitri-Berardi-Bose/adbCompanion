#!/bin/bash

# Defining Directories 
homeDirectory="$HOME"
binDirectory="$homeDirectory/bin/"
adbCompanionDir="$binDirectory/adbCompanion/"
adbCompanionSrc="$adbCompanionDir/src/"

source $adbCompanionSrc/kv-bash/./kv-bash

######## Main Script Starts Here #########

cd $adbCompanionDir
	
eatOutput=$(git stash)

if [[ $(git branch | grep \* | cut -d ' ' -f2) != "master2" ]]; then
	eatOutput=$(git checkout master2) 
	kvset mostRecentBranch $(git branch | grep \* | cut -d ' ' -f2)
else
	kvset mostRecentBranch master2
fi

#eatOutput=$(git pull)
exec adbcompanion "D3gN0" "$@"

exit 1

